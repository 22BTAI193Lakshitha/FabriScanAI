# FabriScan_Pilot
This is the finalized repository to showcase the project and push it to live

cd backend; .\venv_tf\Scripts\Activate.ps1; uvicorn main:app --reload --port 8000
cd frontend; npm run dev